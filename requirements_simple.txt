# Minimal requirements - only essential packages
# No matplotlib, numpy, scipy needed for simple GUI

cryptography>=41.0.0
pycryptodome>=3.19.0
ecdsa>=0.18.0
base58>=2.1.0
aiohttp>=3.9.0

# Note: Python 3.6+ has built-in hashlib.sha3_256, so pysha3 not needed
# tkinter is built-in with Python, no installation needed

